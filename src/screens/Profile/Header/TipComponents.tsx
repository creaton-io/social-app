import {Button, ButtonText} from '#/components/Button'

export default function TipComponents() {
  return (
    <Button
      onPress={() => {}}
      variant="solid"
      color="primary"
      label="Connect Wallet">
      <ButtonText>Tipping soon</ButtonText>
    </Button>
  )
}
